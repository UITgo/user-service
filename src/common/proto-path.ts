import { join } from 'path';

export const USER_PROTO = join(__dirname, '..', '..', 'proto', 'user.proto');
export const DRIVER_PROTO = join(__dirname, '..', '..', 'proto', 'driverstream.proto'); // hoặc driver.proto
